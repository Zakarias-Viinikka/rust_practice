Generate random dots (nodes) and randomly connect them (edges), ensuring no node is unconnected.

Draw it on a canvas.

Implement the graph using the Rc<RefCell<Node>> pattern (each node holds Rc pointers to its neighbors).

After running your reachability algorithm (like BFS within 2 steps), visualize which nodes are reachable on the canvas.
