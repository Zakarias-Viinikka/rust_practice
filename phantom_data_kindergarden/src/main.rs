fn main() {
    println!("Hello, world!");
}

//i have a Col that came from the db. it has just what?
//
/*


#[derive(Serialize, Deserialize, Debug, Clone, uniffi::Enum, PartialEq)]
pub enum Col {
    Null,
    Integer(i64),
    Real(f64),
    Text(String),
    Blob(Vec<u8>),
}



 */

//i want to turn it into T
//
