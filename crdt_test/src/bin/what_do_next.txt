did last: changed mapref to array ref

next:
*being able to insert text at x spot in the mapRef
*being able to edit text at y position in a text_block (which is what the mapRef stores)
*I can create/write to a mapRef (which is where I had previously planned to store my text blocks) and i wrote a function to make sure everything is working the way I expect it to. (should turn that into an automatic test eventually)
