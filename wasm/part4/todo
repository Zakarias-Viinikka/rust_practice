//working on
swapping to cols in different rows.

todo:
*edit_col_in_row //check this one works and stuff. havent tested it and stuff yet. <-- currently in the middle of this
    :ai wrote some weird thing for the actual parameters it takes in, i gotta change that also need to write the create_sql_statement for it.
    update exists, but i gotta make sure it's actually the correct one? idk. just gotta make sure it matches my black_magic_read and check if that one is even using my
    sql_statement creater thing
*delete row //check this one works and stuff too
*edit rows //check if it works
*tests
*make it so sorting by "position in list" is efficient
*either export the db or figure out how to show all of it's info on a page u can just open so it's easy to analyze what the db looks like.
